import { useState, memo } from "react";
import "./style.scss";
import ProductCard from "../productCard";

const ProductCard_Slider = () => {
    return (
        <>
        </>
    );
};

export default memo(ProductCard_Slider);